using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AumentaDificultad : MonoBehaviour
{
    // Start is called before the first frame update
    public static float velocidadDificil = 0;
    void Start()
    {
        velocidadDificil = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
